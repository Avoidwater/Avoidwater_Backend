name: CD

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Set yml file 
      uses: microsoft/variable-substitution@v1
      with:
        files: src/main/resources/application.yaml
      env:
        external.weather.url: ${{ secrets.WEATHER_API_URL }}
        external.weather.key: ${{ secrets.WEATHER_API_KEY }}

    - name: Build with Gradle
      run: ./gradlew build -x test
      
    - name: Build Docker Image For Spring
      run: |
        docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_PASSWORD }}
        docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/avoid-water:latest .
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/avoid-water:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: EC2 Docker Run
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.LINUX_HOST }}
          username: ${{ secrets.LINUX_USERNAME }}
          password: ${{ secrets.LINUX_PASSWORD }}
          port: ${{ secrets.LINUX_SSH_PORT }}
          script: |
            docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_PASSWORD }}
            docker stop avoid-water
            docker rm avoid-water
            docker rmi ${{ secrets.DOCKERHUB_USERNAME }}/avoid-water:latest
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/avoid-water:latest
            docker run -d -p 33334:9991 --name avoid-water ${{ secrets.DOCKERHUB_USERNAME }}/avoid-water:latest
